[project]
name = "nexus-webscraper-mcp"
version = "0.1.0"
description = "Advanced web scraping MCP server powered by Crawl4AI with automatic content trimming."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "crawl4ai>=0.8.6",
    "mcp>=1.26.0",
    "playwright>=1.58.0",
]
